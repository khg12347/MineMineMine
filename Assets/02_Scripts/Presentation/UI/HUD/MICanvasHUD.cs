using MI.Core;
using UnityEngine;

namespace MI.Presentation.UI.HUD
{
    public class MICanvasHUD : MonoBehaviour, IMIHUD
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}